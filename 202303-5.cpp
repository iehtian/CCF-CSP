#include<iostream>
#include<algorithm>
using namespace std;
struct che
{
    int qi, zhong;
};
int main(){
    int n, m;
    cin >> n >> m;
    che c[m];
    for (int i = 0; i < m; i++)
    {
        cin >> c[i].qi >> c[i].zhong;
    }
    for (int i = 0; i < n; i++)
    {
        /* code */
    }
    
}