#include<algorithm>
#include<iostream>
using namespace std;
int main(){
    int n, m;
    cin >> n >> m;
    int dongli[n][n][n] = {0};
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            int tem;
            cin >> tem;
            if (tem==1)
            {
                int l, r, a, b, c;
                cin >> l >> r >> a >> b >> c;
                a[l-1]=
                
            }
            
        }
        
    }
    
}